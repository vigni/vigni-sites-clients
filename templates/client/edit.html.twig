{% extends 'base.html.twig' %}

{% block title %}Modifier un client{% endblock %}

{% block body %}
<div class="index">
    <h1>Modification {{ client.nameCompany }}</h1>
    <div class="index-container">
        {{ include('client/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('client_index') }}">Retour à la liste</a>

        {{ include('client/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
